# Deseo-de-enero-ملف README - موقع أمنيات يناير ✨

📖 نظرة عامة

أمنيات يناير هو موقع ويب تفاعلي يسمح للمستخدمين بكتابة أمنياتهم للعام الجديد خلال شهر يناير فقط من كل سنة. يتميز الموقع بتصميم جذاب بألوان النيون، وواجهة عربية كاملة، ونظام أمان متكامل يحمي خصوصية المستخدمين.

✨ المميزات

🎨 تصميم مذهل

· أنماط نيون متحركة وتأثيرات بصرية جذابة
· نجوم متحركة في الخلفية
· ألوان متناسقة (وردي، أزرق، أصفر، أخضر)
· تصميم متجاوب يعمل على جميع الأجهزة

🔒 نظام أمان متكامل

· كل مستخدم يرى أمنياته فقط
· المدير فقط له صلاحيات كاملة
· منع الكتابة خارج شهر يناير
· منع تكرار الأمنيات في نفس السنة

📱 واجهة مستخدم عربية

· واجهة كاملة باللغة العربية
· سهولة الاستخدام والتنقل
· رسائل تنبيه تفاعلية
· مؤثرات صوتية وبصرية

🛠️ إدارة متقدمة

· لوحة تحكم للمدير
· إحصائيات مفصلة
· إمكانية حذف الأمنيات
· نظام تصفية حسب السنة

🚀 التقنيات المستخدمة

التقنية الوصف
HTML5 هيكل الموقع
CSS3 الأنماط والتنسيق
JavaScript المنطق والتفاعل
Firebase Realtime Database قاعدة البيانات
Firebase Authentication نظام المصادقة
Font Awesome الأيقونات
Google Fonts الخطوط العربية

📁 هيكل الملفات

```
january-wishes/
├── index.html          # الصفحة الرئيسية
├── README.md           # ملف التعليمات
├── firebase-config.js  # إعدادات فايربيز (اختياري)
└── assets/             # المجلد الخاص بالملفات المساعدة
    ├── images/         # الصور
    ├── sounds/         # الأصوات (اختياري)
    └── styles/         # الأنماط الإضافية
```

🔧 متطلبات التشغيل

1. متصفح ويب حديث (Chrome, Firefox, Edge, Safari)
2. اتصال بالإنترنت للوصول إلى Firebase
3. حساب Firebase مجاني

🛠️ الإعداد والتثبيت

الخطوة 1: إنشاء مشروع Firebase

1. اذهب إلى Firebase Console
2. انقر على إضافة مشروع (Add project)
3. أدخل اسم المشروع: january-wishes
4. اختر تمكين Google Analytics (اختياري)
5. انقر على إنشاء المشروع (Create project)

الخطوة 2: إضافة تطبيق ويب

1. في لوحة تحكم Firebase، انقر على </> (إضافة تطبيق ويب)
2. أدخل اسم التطبيق: أمنيات يناير
3. انسخ بيانات التكوين:
   ```javascript
   const firebaseConfig = {
     apiKey: "...",
     authDomain: "...",
     databaseURL: "...",
     projectId: "...",
     storageBucket: "...",
     messagingSenderId: "...",
     appId: "..."
   };
   ```

الخطوة 3: تفعيل الخدمات

أ. قاعدة البيانات Realtime Database

1. من القائمة الجانبية، اختر Realtime Database
2. انقر على إنشاء قاعدة بيانات
3. اختر منطقة قريبة منك (مثل europe-west1)
4. اختر وضع الاختبار (للتجربة)
5. انقر على تم

ب. قواعد الأمان

1. في صفحة Realtime Database، انتقل إلى Rules
2. الصق قواعد الأمان التالية:
   ```json
   {
     "rules": {
       ".read": "auth != null",
       ".write": "auth != null",
       
       "wishes": {
         "$wishId": {
           ".write": "newData.hasChildren(['text', 'userName', 'userId', 'year', 'timestamp', 'userId_year']) && (auth.uid === newData.child('userId').val() || root.child('admins').child(auth.uid).exists())",
           ".validate": "newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length < 1000 && newData.child('userName').isString() && newData.child('userName').val().length > 0 && newData.child('userName').val().length < 100 && newData.child('userId').val() === auth.uid && newData.child('year').isNumber() && newData.child('timestamp').isNumber()"
         }
       },
       
       "userWishes": {
         "$userId": {
           ".read": "auth.uid === $userId || root.child('admins').child(auth.uid).exists()",
           ".write": "auth.uid === $userId"
         }
       },
       
       "users": {
         "$userId": {
           ".read": "auth.uid === $userId || root.child('admins').child(auth.uid).exists()",
           ".write": "auth.uid === $userId && (!data.exists() || data.child('isAdmin').val() === newData.child('isAdmin').val())"
         }
       },
       
       "admins": {
         ".read": "root.child('admins').child(auth.uid).exists()",
         ".write": "root.child('admins').child(auth.uid).exists()"
       },
       
       "settings": {
         ".read": true,
         ".write": "root.child('admins').child(auth.uid).exists()"
       }
     }
   }
   ```
3. انقر على نشر

الخطوة 4: تهيئة الموقع

1. انسخ ملف index.html من الكود المرفق
2. استبدل بيانات Firebase في السطور:
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSyATUn0oNQsY9JG42qBhxGXAYJQiChANgF0",
     authDomain: "january-wish.firebaseapp.com",
     databaseURL: "https://january-wish-default-rtdb.firebaseio.com",
     projectId: "january-wish",
     storageBucket: "january-wish.firebasestorage.app",
     messagingSenderId: "31123732054",
     appId: "1:31123732054:web:b5c1e965702b2554f3edf7",
     measurementId: "G-MBJGST5TFK"
   };
   ```
3. احفظ الملف

الخطوة 5: رفع الموقع

اختر إحدى الطرق التالية:

أ. الاستضافة المحلية

· افتح ملف index.html مباشرة في المتصفح
· يعمل بشكل كامل مع اتصال إنترنت

ب. GitHub Pages (مجاني)

1. أنشئ مستودعاً جديداً على GitHub
2. ارفع ملف index.html
3. اذهب إلى Settings → Pages
4. اختر main branch كفرع النشر
5. انتظر دقيقة ثم افتح الرابط

ج. Netlify (مجاني)

1. سجل الدخول إلى Netlify
2. اسحب وأسقط ملف index.html
3. احصل على الرابط مباشرة

📊 هيكل قاعدة البيانات

```json
{
  "settings": {
    "siteName": "✨ أمنيات يناير ✨",
    "currentYear": 2024,
    "isJanuary": true,
    "allowNewWishes": true
  },
  
  "admins": {
    "admin_uid": true
  },
  
  "users": {
    "user_id": {
      "name": "اسم المستخدم",
      "email": "email@example.com",
      "isAdmin": false,
      "createdAt": 1234567890000
    }
  },
  
  "wishes": {
    "wish_id": {
      "text": "نص الأمنية",
      "userName": "اسم المستخدم",
      "userId": "user_id",
      "year": 2024,
      "timestamp": 1234567890000,
      "date": "١ يناير ٢٠٢٤",
      "time": "١٢:٠٠ مساءً",
      "month": "يناير",
      "userId_year": "user_id_2024"
    }
  },
  
  "userWishes": {
    "user_id": {
      "wish_id": {
        // نفس بيانات wish
      }
    }
  }
}
```

👥 أدوار المستخدمين

1. المستخدم العادي

· الصلاحيات:
  · كتابة أمنية واحدة في السنة (فقط في يناير)
  · مشاهدة أمنياته السابقة فقط
  · لا يستطيع رؤية أمنيات الآخرين
· التسجيل: تلقائي عند كتابة أول أمنية

2. المدير

· الصلاحيات:
  · رؤية جميع الأمنيات
  · حذف أي أمنية
  · مسح جميع الأمنيات
  · الدخول في أي وقت (ليس فقط يناير)
· الدخول: باستخدام كلمة سر المدير

🔑 كلمات سر المدير الافتراضية

```javascript
const ADMIN_PASSWORDS = {
  "admin2025": true,
  "admin@january": true,
  "january_wishes_admin": true,
  "السلام_عليكم_2024": true
};
```

تغيير كلمات السر: عدل المصفوفة ADMIN_PASSWORDS في الكود

🎯 دليل الاستخدام

للمستخدم العادي:

1. افتح الموقع في يناير
2. انقر على كتابة أمنية جديدة
3. أدخل اسمك وأمنيتك
4. انقر على إرسال
5. لعرض أمنياتك السابقة، انقر على مشاهدة أمنياتي السابقة

للمدير:

1. افتح الموقع في أي وقت
2. انقر على دخول كمدير
3. أدخل كلمة السر
4. استخدم لوحة التحكم لإدارة الأمنيات

⚙️ الإعدادات القابلة للتخصيص

1. الألوان

عدل متغيرات CSS في بداية الملف:

```css
:root {
  --primary: #ff00ff;    /* اللون الوردي */
  --secondary: #00ffff;  /* اللون الأزرق */
  --accent: #ffff00;     /* اللون الأصفر */
  /* ... */
}
```

2. الرسائل

عدل نصوص الرسائل في الدوال:

```javascript
function showAlert(message, type) {
  // عدل هنا
}
```

3. القيود

```javascript
// الحد الأقصى لطول الأمنية
const MAX_WISH_LENGTH = 500;

// عدد الأمنيات المسموح به لكل مستخدم في السنة
const WISHES_PER_YEAR = 1;
```

🔍 استكشاف الأخطاء وإصلاحها

المشكلة الحل
الموقع لا يفتح تأكد من اتصال الإنترنت
لا يمكن كتابة أمنية تأكد أن التاريخ هو شهر يناير
خطأ في قاعدة البيانات تأكد من قواعد الأمان في Firebase
التصميم غير متجاوب تأكد من استخدام متصفح حديث
الرسائل لا تظهر تأكد من تفعيل JavaScript

📈 إضافة بيانات تجريبية

لإضافة بيانات تجريبية، استخدم هذا الكود في console المتصفح:

```javascript
// أضف بيانات تجريبية
const sampleData = {
  wishes: {
    "wish_1": {
      text: "أتمنى أن أتعلم لغة جديدة هذا العام",
      userName: "أحمد",
      userId: "ahmed123",
      year: 2024,
      timestamp: Date.now(),
      date: "١ يناير ٢٠٢٤",
      time: "١٢:٠٠ مساءً",
      month: "يناير",
      userId_year: "ahmed123_2024"
    }
  }
};

// استبدل بمرجع قاعدة البيانات الخاص بك
firebase.database().ref().update(sampleData);
```

🔄 التحديثات المستقبلية

1. ميزات مقترحة:
   · إضافة إمكانية التعديل على الأمنيات
   · نظام الإعجاب والتعليقات
   · مشاركة الأمنيات على وسائل التواصل
   · تقارير إحصائية متقدمة
   · نظام الإشعارات
2. تحسينات الأداء:
   · تحسين سرعة التحميل
   · إضافة Service Worker للتخزين المؤقت
   · تحسين تجربة المستخدم على الأجهزة البطيئة

👥 المساهمة في المشروع

1. انسخ المشروع (Fork)
2. أنشئ فرعاً جديداً (git checkout -b feature/AmazingFeature)
3. احفظ التغييرات (git commit -m 'Add some AmazingFeature')
4. ادفع إلى الفرع (git push origin feature/AmazingFeature)
5. افتح طلب دمج (Pull Request)

📄 الرخصة

هذا المشروع مرخص تحت رخصة MIT - انظر ملف LICENSE للتفاصيل.

👨‍💻 المطور

فريق أمنيات يناير
📧 البريد الإلكتروني: support@january-wishes.com
🌐 الموقع: january-wishes.com

🙏 الشكر

· فريق Firebase للمنصة الرائعة
· Font Awesome للأيقونات الجميلة
· Google Fonts للخطوط العربية
· جميع المساهمين والمستخدمين

---

✨ تذكر: الأمنيات تتحقق بالإيمان والعمل، فكن متفائلاً واعمل بجد لتحقيق أحلامك!
